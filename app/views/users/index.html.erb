<h1 id="notice"><%= notice %><h1>
<h1>Chat with Users</h1>
<% if @users %>
<ul>
<% @users.each do |user| %>
  <li><%= link_to "#{user.id} #{user.email}", new_chat_path(:id => user.id) %></li>
<% end %>
</ul>
<% end %>
